services:
  - type: web
    name: maestro-twin
    runtime: python
    plan: free
    buildCommand: ""
    startCommand: "python3 run.py --host 0.0.0.0 --port $PORT --no-browser"
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: "3.11.9"
